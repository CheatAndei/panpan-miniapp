# 番番记录 UI 画像

更新：2026-07-28
状态：全项目浅蓝视觉、绿色计算打卡海报与无回弹照片手势已实现并完成代码、测试、构建与关键流截图验收；微信体验版 `1.07.290020` 已上传，生产后端 `panpan-v2.3.0` 保持在线

## 产品与用户

- 产品：教师与家长双角色的微信小程序。
- 教师核心任务：看今日待办、签到、发反馈、批改打卡、管理学习小组与教学工具。
- 家长核心任务：看孩子今日状态、学习任务、反馈、成长记录与练习结果。
- 学习场景：日常练习强调清晰与低压力；挑战、排行榜、成就海报允许更强的节奏与奖励感。

## 技术画像

- Vue 3 + uni-app + Vite，目标端为 `mp-weixin`。
- 共 41 个页面，当前无分包；底栏仅“首页 / 我的”。
- 全局样式与 tokens 位于 `App.vue`。
- 共享 UI：`pp-icon`、`pp-state`、`pp-avatar`、`pp-question-reader` 等。
- 图标：21 枚本地 SVG、4 枚 TabBar PNG；不依赖图标库。
- 动效：`pp-icon` 提供 `pop/ring/shine/breathe/bob` 五类语义动效，页面另有短促进入、弹层、按压与进度反馈，统一支持 reduced-motion。

## 现有设计基因

- 主色：清亮学习蓝、行动蓝。
- 表面：浅蓝作业纸、纸白、浅蓝选中底。
- 语义色：珊瑚承担提醒、纠错与老师消息，深珊瑚承担错误与危险操作。
- 形态：中等圆角、轻边框、克制阴影、移动端大触控区。
- 品牌气质：可靠、平静、熟人老师、家校沟通。

### 必须保留

- “番番记录”品牌名；浅蓝教育系统作为主界面识别色，森林绿不再作为主色。
- 教师效率、家长安心、学生学习三个场景的差异。
- 现有业务规则、API、路由、角色、数据与存储逻辑。
- 本地图标与轻量依赖策略。
- 反馈卡、宣传海报、打卡海报、口算战绩保留不同信息结构；计算打卡海报使用独立绿色主题，口算与压轴海报保留旧版主题。

### 应避免

- 全站套同一种卡片，抹平角色差异。
- 紫色渐变、玻璃拟态、霓虹、巨大圆角、持续漂浮。
- GSAP、Lottie 或新大型 UI/动效依赖。
- 依赖动画理解状态；自动音效、频繁震动、长时间粒子动画。
- 用黄、绿、蓝、黑四色拼贴制造“活力”，或让大面积深色重新成为主界面底色。

## 审计结论

### 已完成：全项目可见视觉迁移

- 41 个注册页面、共享组件、本地 SVG 与 TabBar PNG 均收敛到浅蓝白、学习蓝、薄荷绿与珊瑚体系；计算打卡 Canvas 单独使用绿色同色系，口算与压轴 Canvas 保留旧版界面。
- 家长首页围绕孩子、今日任务、课后任务、成长和家校服务连续阅读，当前主视图含 21 个本地图标；所有家长相邻功能页均补齐标题、状态与操作图标。
- 教师首页将快捷工作置顶并扩为 6 项，新增“学习记录”；教师运营页保持高扫描密度，批改台支持最近 3 份、修改、预览和保存。
- 学生挑战、排行、成长与结果页使用图标、进度、徽章、错峰入场和一次性反馈获得活力，主界面不使用四色拼贴。
- 批改海报使用绿色侧脊、薄荷数据卡与白纸构图并移除隐私说明，三组文案池各 100 条且洗牌周期内不重复；口算与压轴海报保留各自旧主题。

### 已完成：全局留白根因修复

- `App.vue` 对页面原语统一 `box-sizing: border-box`，原生 `button` 使用稳定的 flex 垂直居中并清除默认纵向 padding。
- 批改台翻页、缩放、旋转、答案卡和学生导航不再叠加“固定高度 + 上下 padding”；无照片状态压缩，有照片时才保留稳定画布。
- `pp-state` 已移除固定 `236rpx` 容器高度，短加载、空态和错误态按真实内容自然收高。
- 学习中心“今日 / 学习 / 成长”导航从 `112rpx` 收敛到 `76rpx`，并加入日历、书本和成长图标。

### 已完成：图标与动效语言

- `pp-icon` 已支持装饰/语义无障碍属性、错峰延迟以及 `pop/ring/shine/breathe/bob` 五类 transform/opacity 动效。
- 家长、教师、学生和公共功能页均有本地图标锚点；动画只绑定当前任务、提醒、完成、奖励和目标，循环卡片保持静止。
- 所有非必要动效支持 `prefers-reduced-motion: reduce`，不新增 GSAP、Lottie 或大型 UI 依赖。

## 推荐方向

`清爽校园文具 × 教师高效率 × 家长安心 × 学习区轻活力`

- 统一底座：浅蓝白、学习蓝与少量薄荷绿/珊瑚红；大面积保持浅色，黄色不承担主界面卡片和主行动。
- 教师端：高信息密度、任务优先、明确待办和批量操作。
- 家长端：更温暖、可读、围绕孩子今日与成长叙事。
- 学习挑战：用进度、图标、一次性结果动效和少量珊瑚提醒强化奖励感，不用四色拼贴。
- 教育辨识：通过练习册页签、纸张分隔线、书本/铅笔/尺规/公式图标建立，不靠堆放卡通人物或 Emoji。

### 浅色教育色板提案

| 角色 | 色值 | 用途 |
|---|---|---|
| 页面底色 | `#F6FAFF` | 浅蓝作业纸背景 |
| 纸张白 | `#FFFFFF` | 卡片、表单、弹层 |
| 学习蓝 | `#527CC9` | 品牌、进度、选中 |
| 行动蓝 | `#315EA8` | 主按钮、链接与关键数据 |
| 浅蓝 | `#EAF2FF` | 选中底、图标容器 |
| 薄荷绿 | `#65BFA8` | 正确、完成与通过 |
| 珊瑚红 | `#E98577` | 提醒、纠错、老师消息与少量点睛 |
| 浅珊瑚 | `#FFF0ED` | 待办、纠错与提醒底色 |
| 深蓝灰文字 | `#24324A` | 正文与标题 |
| 次级文字 | `#5C6C84` | 说明文字 |
| 分隔线 | `#DDE7F2` | 边框、纸张分隔 |

主界面不再使用大面积深绿，也不使用黄、绿、蓝、黑同时争抢层级。深色只保留在正文、结果数据和用户明确要求恢复的旧版口算海报中。

## 动效能力边界

- 可用：CSS/WXSS `transform + opacity`、`uni.createAnimation`、`swiper` / `movable-view`。
- 不用：GSAP、Lottie、Vue `<Transition>`、自定义小程序跨页转场。
- 节奏：
  - 按压与选中：120–180ms。
  - Tab、筛选、进度：160–220ms。
  - 弹层：遮罩 180ms，内容 220–260ms。
  - 成功反馈：180–240ms，可选一次轻触震动。
  - 庆祝：单次 1.2–1.8s，可跳过；循环装饰严格限量。
- 所有非必要动效支持减少动态效果。

## 验证画像

- 前端全量：`npm test` 158/158 通过，包含微信 WXSS 禁用通配选择器回归。
- 新增后端学习记录：4/4 通过，覆盖教师归属隔离、家长禁止访问、五类题量聚合与学生题库。
- 后端全量：116 项中 112 项通过；4 项既有压轴挑战测试因仓库缺少 `backend/resources/weekly-challenges/manifest.json` 失败，与本次改动无关，保留为资源风险。
- 构建：`npm run build:icons`、`npm run build:h5`、`npm run build:mp` 均通过。
- 小程序检查：`npm run check:mp` 通过，AppID 为 `wx77f8ea684b6420ca`，生产 API 为 `https://panpan.xpytt.com/api`。
- 微信 CI 服务端编译与上传通过：体验版 `1.07.290020`，完整包 `1567627` 字节，无插件包。
- 新后端标识：`panpan-v2.3.0`，包含教师学生学习记录聚合与题库接口；生产备份/回滚工作流 `30357380120` 发布成功，线上健康检查通过。
- 390×844 批改台实测无页面横向溢出；空照片区约 146px，翻页与图片操作按钮无多余纵向白块；答案题卡仅在内部横向轨道滚动。
- 390px 家长首页、教师首页、学生记录、学习中心、批改台与三类海报已留存截图；当前主界面统一为浅蓝教育视觉。
- 仍需微信开发者工具真机确认系统字体差异、相册权限弹窗、原生 `movable-view` 惯性及真实私图下载，然后以体验版完成最终设备验收。
