# 微信小程序体验版上传记录

## 2026-07-16 极速批改版

- 上传结果：成功
- 上传时间：2026-07-16 00:57（Asia/Shanghai）
- AppID：`wx77f8ea684b6420ca`
- 版本号：`1.07.160057`
- 版本说明：`极速批改-左右对照只点错题`
- CI 机器人：`1`
- 微信完整包大小：`522714` 字节
- 构建目录：`dist/build/mp-weixin`
- 构建文件数：`108`
- 生产 API：`https://panpan.xpytt.com/api`
- 上传私钥文件：`private.wx77f8ea684b6420ca.key`
- 上传出口 IP：`172.96.141.61`

> 本文只记录私钥文件路径，不记录或复制私钥内容。

## 本版内容

- 教师批改页改为左侧学生照片、右侧标准答案的对照布局。
- 未点击题目默认判定正确，教师只需点击错题。
- 错题同时使用文字和颜色标记，避免仅靠颜色传达状态。
- 保存复核后自动进入下一位待批改学生。
- 照片自动鉴权下载，可左右切换并点击放大。
- 打卡题库统一为固定初中计算题，不提供难度调整选项。
- 固定计算题库共 `960` 道，超过需求的至少 `800` 道。

## 上传前校验

- 前端测试：`29/29` 通过。
- 后端测试：`44/44` 通过。
- 微信生产构建：成功。
- 微信上传前检查：AppID、生产 API、项目配置、私钥文件均通过。
- 微信上传回执：`ok: true`，完整包 `522714` 字节，无插件包。

## 后续上传约定

1. 构建使用 WSL Ubuntu：

   ```bash
   cd /mnt/d/biancheng/qian/teach/panpan
   npm run build:mp
   npm run check:mp
   ```

2. 微信 CI 本地编译使用 Windows Node。当前 `miniprogram-ci@2.1.31` 在 WSL 挂载盘上会出现 `fork process timeout`，Windows Node 已验证可正常完成。

3. 上传时保持 TNT 连接，并让 `HTTP_PROXY`、`HTTPS_PROXY` 指向 `http://127.0.0.1:7892`。微信后台白名单已加入本次出口 IP `172.96.141.61`。

4. 代理节点的公网 IP 可能由服务商调整。上传前应复核出口 IP；若不再是 `172.96.141.61`，先更新微信上传白名单，避免把 IP 错误误判为私钥错误。

5. 上传命令示例：

   ```powershell
   node scripts/mp-ci-upload.js --version=1.07.160057 --desc=极速批改-左右对照只点错题
   ```
